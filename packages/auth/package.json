{
  "name": "@repo/auth",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "generate": "bun x @better-auth/cli generate --config ./src/index.cli.ts"
  },
  "dependencies": {
    "@better-auth/prisma-adapter": "^1.5.1",
    "@better-fetch/fetch": "catalog:",
    "@repo/db": "workspace:*",
    "@repo/env": "workspace:*",
    "better-auth": "catalog:",
    "better-auth-mp": "workspace:*",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@repo/config": "workspace:*",
    "typescript": "catalog:"
  }
}

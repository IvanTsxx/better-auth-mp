name: better-auth-mercadopago

services:
  postgres:
    image: postgres
    container_name: better-auth-mercadopago-postgres
    environment:
      POSTGRES_DB: better-auth-mercadopago
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - better-auth-mercadopago_postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  better-auth-mercadopago_postgres_data:
